---
title: "Untitled"
output: pdf_document
date: "2023-11-29"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

1.  

```{r}
library(rvest)
library(dplyr)
library(polite)

url <- "https://www.imdb.com/chart/toptv/?ref_=nv_tvv_250"

session <- bow(url,user_agent = "Educational Purposes")
session

```

1.  

```{r}
title <- scrape(session)%>%
html_nodes("h3.ipc-title__text")%>%
html_text
title

subsettt <- as.data.frame(title[2:51])
colnames(subsettt) <- "title"
split_df <- strsplit(as.character(subsettt$ranks), ".", fixed = TRUE )
split_df<- data.frame(do.call(rbind, subsettt))
subsettt

rate <- scrape(session)%>%
html_nodes("span.ipc-rating-star--imdb")%>%
html_text

subrate <- as.data.frame(rate[1:50])
colnames(subrate) <- "rating"
split_df2 <- strsplit(as.character(subrate$voteCount), ".", fixed = TRUE )
split_df2 <- data.frame(do.call(rbind, subrate))
subrate

midata <- data.frame(
  title = subsettt,
  rate = rate
)
midata

  

```

1.  

```{r}
eps <- scrape(session)%>%
html_nodes("div.sc-479faa3c-7.jXgjdT.cli-title-metadata")%>%
html_text
eps

subset3 <- as.data.frame(eps[1:50])
colnames(subset3) <- "year"
split_df3 <- strsplit(as.character(subset3$episodes), ".", fixed = TRUE )
split_df3 <- data.frame(do.call(rbind, subset3))

subset3


```

1.  

```{r}

```
